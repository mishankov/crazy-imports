version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  lint:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - run:
          command: pip install black
          name: Install black
      - run:
          command: black --check .
          name: Run black
  test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - run:
          command: pip install pytest
          name: Install pytest
      - run:
          command: cd tests
          name: Navigate to tests directory
      - run:
          command: python utils/init_db.py
          name: Initialize test database
      - run:
          command: python -m pytest
          name: Run tests

workflows:
  main:
    jobs:
      - lint
      - test
      - hold:
          type: approval
          requires:
            - lint
            - test
